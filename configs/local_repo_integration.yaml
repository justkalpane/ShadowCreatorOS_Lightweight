version: 3
mode: hybrid_github_local
constitutional_authority_ref: docs/01-architecture/canonical_authority_precedence.md
wave_00_update_note: >
  Version 3 adds constitutional authority reference and decision packet flags.
  The runtime_repo_access value is aligned to the canonical local clone posture.

repo_source_of_truth: GitHub
runtime_repo_access: local_clone_preferred
sync_mode: manual_command_driven

expected_windows_repo_root: C:\ShadowEmpire\Shadow-Creator-OS-Phase_01

paths:
  workflows_root: n8n/workflows
  manifests_root: n8n/manifests
  schemas_root: schemas
  rules_root: rules
  registries_root: registries
  bindings_root: bindings
  skills_root: skills

consumption_rules:
  - GitHub is the canonical source of truth
  - local repo is runtime-readable mirror
  - no automatic sync required
  - sync happens only on command

agent_behavior:
  - agent reads live GitHub
  - agent updates local mirror only when instructed

decision_packet_flags:
  - DP-003: runtime_repo_access value aligned between config and validator
  - DP-002: data table names in this config may need updating after table name reconciliation
