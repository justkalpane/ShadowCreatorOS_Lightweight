workflow_id: CWF-220
completion_packet_emission: required
required_node_contract:
  - trigger_node
  - dossier_load_node
  - registry_lookup_node
  - skill_execution_node
  - packet_validation_node
  - append_only_dossier_write_node
  - packet_index_write_node
  - wf900_error_routing_node
  - next_stage_routing_node
  - completion_packet_emission_node
wf900_error_route: WF-900
execution_contract: executable_json_required
legacy_workflow_json: n8n/workflows/script/CWF-220-script-debate.json
canonical_workflow_json: n8n/workflows/CWF-220.json
name: cwf220_script_debate
class: child
parent_pack: WF-200
purpose: challenge the script draft for weak hooks, weak structure, unsupported claims, and retention risks
primary_owner: Krishna
supporting_directors:
  - Durga
  - Yama
  - Saraswati
inputs:
  - script_draft_packet
output_packet_family:
  - script_debate_packet
writes:
  - namespaces.script
  - namespaces.approval
write_targets:
  dossier_namespaces:
    - script
    - approval
decision_outputs:
  - APPROVED_FOR_REFINEMENT
  - REJECTED
  - NEEDS_REWRITE
checks:
  - hook_strength
  - structure_strength
  - evidence_support
  - retention_flow
  - governance_safety
done_criteria:
  - critique log emitted
  - decision emitted
  - rewrite reasons attached when needed
fallback_behavior:
  on_rejected_draft:
    - return to CWF-210 with critique reasons
