workflow_id: CWF-230
completion_packet_emission: required
required_node_contract:
  - trigger_node
  - dossier_load_node
  - registry_lookup_node
  - skill_execution_node
  - packet_validation_node
  - append_only_dossier_write_node
  - packet_index_write_node
  - wf900_error_routing_node
  - next_stage_routing_node
  - completion_packet_emission_node
wf900_error_route: WF-900
execution_contract: executable_json_required
legacy_workflow_json: n8n/workflows/script/CWF-230-script-refinement.json
canonical_workflow_json: n8n/workflows/CWF-230.json
name: cwf230_script_refinement
class: child
parent_pack: WF-200
purpose: refine the debated script draft by resolving critique points and improving clarity, retention, and support
primary_owner: Saraswati
supporting_directors:
  - Krishna
  - Vyasa
inputs:
  - script_debate_packet
output_packet_family:
  - script_refinement_packet
writes:
  - namespaces.script
write_targets:
  dossier_namespaces:
    - script
steps:
  - normalize critique instructions
  - revise hook, sections, and claim phrasing
  - strengthen evidence links
  - improve flow and clarity
  - emit refinement packet
done_criteria:
  - critique points resolved or explicitly carried forward
  - refinement packet emitted
fallback_behavior:
  on_unresolved_critical_issues:
    - return to CWF-210 with narrowed rewrite brief
