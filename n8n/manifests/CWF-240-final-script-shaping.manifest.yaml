workflow_id: CWF-240
completion_packet_emission: required
required_node_contract:
  - trigger_node
  - dossier_load_node
  - registry_lookup_node
  - skill_execution_node
  - packet_validation_node
  - append_only_dossier_write_node
  - packet_index_write_node
  - wf900_error_routing_node
  - next_stage_routing_node
  - completion_packet_emission_node
wf900_error_route: WF-900
execution_contract: executable_json_required
legacy_workflow_json: n8n/workflows/script/CWF-240-final-script-shaping.json
canonical_workflow_json: n8n/workflows/CWF-240.json
name: cwf240_final_script_shaping
class: child
parent_pack: WF-200
purpose: convert the refined script into a final production-ready script packet for downstream context engineering
primary_owner: Krishna
supporting_directors:
  - Saraswati
  - Durga
inputs:
  - script_refinement_packet
output_packet_family:
  - final_script_packet
writes:
  - namespaces.script
  - namespaces.runtime
write_targets:
  dossier_namespaces:
    - script
    - runtime
steps:
  - finalize title and hook
  - finalize section order
  - confirm evidence support
  - prepare production-ready script packet
  - emit final script packet
done_criteria:
  - final script packet emitted
  - evidence references preserved
  - promotion to WF-300 is possible
fallback_behavior:
  on_final_quality_failure:
    - return to CWF-230 for another refinement cycle
