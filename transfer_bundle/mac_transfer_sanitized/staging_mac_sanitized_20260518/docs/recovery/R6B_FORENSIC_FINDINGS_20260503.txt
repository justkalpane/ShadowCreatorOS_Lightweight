================================================================================
PHASE R6B-FORENSIC FINDINGS
================================================================================

Date: 2026-05-03

ISSUE IDENTIFIED:
Workflow JSON import failed with: SQLITE_CONSTRAINT: NOT NULL constraint failed: workflow_entity.id

ROOT CAUSES FOUND:
1. Only 2 out of 37 canonical workflow JSON files have top-level "id" field
   - WF-022.json: has id (wqM2V7L9hK4pT8xC)
   - WF-023.json: has id (rN4bQ1Y6mJ8tC3vP)
   - 35 others: MISSING id field

2. CRITICAL: Parent workflows reference child workflows by hardcoded workflow IDs
   - WF-010.json references 7 child workflows using hardcoded IDs (e.g., dew5Cgkwi8MUtAH7)
   - WF-100.json references 4 child workflows using hardcoded IDs (e.g., 9aM3CMjyQbaO9XYE)
   - WF-200.json references 4 child workflows using hardcoded IDs
   - WF-300.json references 4 child workflows using hardcoded IDs
   - WF-400.json references 4 child workflows using hardcoded IDs
   - WF-500.json references 3 child workflows using hardcoded IDs
   - WF-600.json references 3 child workflows using hardcoded IDs

   These IDs are NOT the same as workflow names (WF-001, CWF-110, etc.)
   When importing without IDs, n8n auto-generates new IDs
   New IDs will NOT match the hardcoded references in parent Execute Workflow nodes
   Result: Broken workflow execution chain after import

SAFE REPAIR OPTIONS:

Option A — Temporary Import Copies with ID Generation + Reference Mapping
   Process:
   1. Copy all 37 root workflow JSON files to temporary folder
   2. Add auto-generated IDs to files that are missing them
   3. Create a mapping: generated_id -> original_workflow_name
   4. Find all Execute Workflow nodes in parent workflows
   5. Update their hardcoded workflowId references to match generated IDs
   6. Import from temporary folder
   7. Keep original source files untouched
   
   Pros:
   - Original source files remain unchanged
   - Full control over ID generation and reference mapping
   - Can verify mapping before import
   - Automated and auditable
   
   Cons:
   - Requires reading/modifying 35+ workflow JSON files (in temp only)
   - Mapping logic is complex but manageable
   - Temp files must be created and cleaned after import
   
   Risk Level: MEDIUM (complex mapping, but temp files only)

Option B — n8n UI Import with Auto-ID-Generation
   Process:
   1. Use n8n UI to import workflows one-by-one
   2. Trust n8n UI to auto-generate IDs
   3. After import, check if Execute Workflow references broke
   4. Manually fix broken references in n8n UI if needed
   
   Pros:
   - No source file modification
   - UI may handle reference mapping automatically
   
   Cons:
   - Slow for 37 workflows (manual UI interaction)
   - Unknown if UI auto-generates IDs or handles parent references
   - Risk of broken workflow chain requiring manual repair in UI
   - Less auditable than programmatic approach
   
   Risk Level: HIGH (unknown UI behavior, potential manual remediation)

Option C — n8n REST/API Import with ID Query-and-Remap
   Process:
   1. Generate IDs for missing workflows
   2. Import each workflow via REST API
   3. Query n8n database to confirm assigned IDs
   4. Update parent workflow Execute Workflow nodes via API
   5. Re-import parent workflows with updated references
   
   Pros:
   - API-based and automated
   - Can verify ID assignment immediately after import
   - Remap references via API before finalizing
   
   Cons:
   - Requires n8n API authentication/token
   - More complex orchestration
   - User has not confirmed API access
   
   Risk Level: MEDIUM (requires auth, but fully automated)

Option D — Direct Source File Patching (NOT APPROVED NOW)
   Process:
   1. Add IDs directly to source workflow JSON files
   2. Update all Execute Workflow references in source files
   3. Commit changes to git
   4. Import from modified source
   
   Pros:
   - Source files become "complete" and self-contained
   - Can be version-controlled
   
   Cons:
   - Modifies user's source repository
   - IDs are auto-generated (not meaningful)
   - Risk of git conflicts if rebuilding from Codex
   - Requires explicit user approval (not given)
   
   Risk Level: HIGH (source modification, requires separate approval)

Option E — Direct SQLite Insert (FORBIDDEN)
   Not recommended. Bypasses n8n validation and audit trail.

RECOMMENDATION:

Option A — Temporary Import Copies with ID Generation + Reference Mapping

Rationale:
- Safest approach: Original source files remain untouched and version-controlled
- Full control over ID generation and reference mapping
- Can be automated with Python script
- Audit trail: Can log all generated IDs and mappings
- If import fails, can retry or revert without touching original files
- Matches user's guidance: "Do NOT modify source workflow JSON files"

Implementation Steps:
1. Create temp folder: C:\ShadowEmpire-Git_Restore_01\temp\n8n_import_r6b\
2. Copy all 37 root canonical JSON files to temp folder
3. Generate 16-character alphanumeric IDs for 35 files missing them
4. Add "id" field to each of 35 files in temp folder
5. Create ID mapping table: workflow_name -> generated_id
6. Find all Execute Workflow nodes in parent workflows (WF-010, WF-100, etc.)
7. Update all hardcoded workflowId references to use generated IDs
8. Import from temp folder using n8n CLI
9. Verify all 37 workflows imported with correct references
10. Preserve temp folder and mapping for R7 (activation) verification
11. Clean up temp files after R8 (registry update) completion

Approval Needed: User must approve Option A before proceeding to R6B retry.

================================================================================
